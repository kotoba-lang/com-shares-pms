{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([shares-pms.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'shares-pms.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
